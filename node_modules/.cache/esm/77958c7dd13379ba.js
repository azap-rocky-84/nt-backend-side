let multer,path;_d13‍.x([["uploadPicture",()=>uploadPicture]]);_d13‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_d13‍.w("path",[["default",["path"],function(v){path=v}]]);

const storage = multer.diskStorage({
    destination: (req, file, cb) => {
        cb(null, path.join(__dirname, "../uploads"));
    },
    filename: (req, file, cb) => {
        cb(null, `${Date.now()}-${file.originalname}`);
    }
});
const uploadPicture = multer({
    storage: storage,
    limits: {
        fileSize: 1 * 1000000
    },
    fileFilter: function(req, file, cb){
        let ext = path.extname(file.originalname);
        if(ext!=='.png' && ext!=='.jpg' && ext!=='.jpeg' && ext!=='.JPG'){
            return cb(new Error("Ammesse solo immagini"));
        }
        cb(null, true);
    },
});
